upstream web {
  server web:8000;
}

# portal
server {

  location /static/ {
     autoindex on;
     #alias /static/;
     root /srv/web/;
  }  
  location / {
        proxy_pass http://web/;
    }
  listen 80;
}